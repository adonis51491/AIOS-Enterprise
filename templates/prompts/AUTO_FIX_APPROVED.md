# AIOS AUTO FIX ??APPROVED IMPLEMENT

The previous PLAN is approved.

Rules:
- Modify only approved files.
- Do not modify `.env`, package files, lock files, or `.gitignore`.
- Run build and `git diff --check`.
- Commit and push only after validation passes.
- Create PR, never merge.

Report commit SHA, PR link, validation results, and remaining risks.
