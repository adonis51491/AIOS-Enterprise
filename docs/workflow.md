# Workflow Engine

Workflows are defined in YAML and executed by the Kernel.

Default workflow:

```yaml
workflow:
  - plan
  - implement
  - review
  - verify
  - release
```

AI agents should not invent workflow steps.
