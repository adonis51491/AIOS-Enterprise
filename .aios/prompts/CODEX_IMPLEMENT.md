# AIOS IMPLEMENT

Follow `AGENTS.md`.

The previous PLAN is approved. Implement only the selected Bug and only the approved files.

Do not install packages, edit `.env`, edit package or lock files, commit, or push.

Stop and ask for approval before migration, dependency change, expanded scope, or additional files.

After implementation, run relevant validation and report evidence.
